from lib.intent_classification.intent_classifier import Classifier

class ClassifierMRDA(Classifier):
    # def __init__(self, model:):
    #     super().__init__()
    #     self.model = model
    #
    # def predict(self, embedding):
    #     self.model.predict(embedding)